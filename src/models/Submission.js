export default class Submission {
    constructor(id, title, userName, verdict, submitTime) {
        this.id = id;
        this.title = title;
        this.userName = userName;
        this.verdict = verdict;
        this.submitTime = submitTime;
    }
}